//write your c

#include <stdio.h>

int main() {
	int n, digit, sum = 0;

	scanf("%d", &n);

	if (!(n >= 10000 && n <= 99999)){
		printf("Invalid input");
		return (0);
}

	while ( n > 0 ){

	digit = n % 10;
	sum += digit;
		n /= 10;

	}

	printf("%d", sum);
}
